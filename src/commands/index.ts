import * as Son from './son'
import * as Quid from './quid'
import {ChatInputCommand} from "../types/ChatInputCommand";

export {
  Son,
  Quid,
}

export default [
  Son,
  Quid,
] as ChatInputCommand[]
